import styled from "styled-components";

export const AreaHeader = styled.header`

    text-align: center;

        ul{
            display: flex;
            list-style: none;
            justify-content: flex-end;
            align-items: center;
        }

        li{
            margin-right: 20px;
            color: #FFD700
        }

`;